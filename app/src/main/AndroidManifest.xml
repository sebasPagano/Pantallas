<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.alumno.pantallas">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        >
        <activity
            android:name=".menu.Menu"
            android:label="Menu"
            android:theme="@style/Italic">
        </activity>

        <activity
            android:name=".registro.Registro"
            android:theme="@style/AppTheme"  >
            </activity>

        <activity
            android:name=".login.MainActivity"
            android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".pedido.Pedidos" android:label="Mi Pedido" android:theme="@style/Italic">


        </activity>
    </application>

</manifest>